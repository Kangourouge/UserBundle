{% extends '@KRGMessage/Default/mail.html.twig' %}
{% trans_default_domain 'mails' %}

{% block content %}
    {{ 'invitation.message'|trans({
        '%url%': absolute_url(path('krg_user_guess_token', {token: user.invitationToken})),
        '%name%': user.lastname
    }, 'mails')|raw|nl2br }}
{% endblock %}
