{% extends '@KRGUser/default/layout.html.twig' %}
{% trans_default_domain 'KRGUserBundle' %}

{% block container %}
    <p>{{ 'registration.confirmed'|trans({'%firstname%': user.firstname}) }}</p>

    {% if targetUrl %}
        <p><a href="{{ targetUrl }}">{{ 'registration.back'|trans }}</a></p>
    {% endif %}
{% endblock %}
